# CUSTOMIZE: fill in your production values before running the deploy script.
DATABASE_URL=postgresql://hq:change_me@localhost:5432/hq_prod?schema=public
NODE_ENV=production
PORT=3002
NEXT_PUBLIC_APP_URL=https://workshop.example.com

# Auth / bootstrap
SUPERADMIN_EMAIL_ALLOWLIST=admin@example.com
SEED_EMAIL=admin@example.com
SEED_PASSWORD=changeme

# Branding (optional — defaults to `hq`).
# COOKIE_PREFIX=hq
# API_KEY_PREFIX=hq_
